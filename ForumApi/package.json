{
  "name": "korsancim-frontend-build",
  "private": true,
  "version": "1.0.0",
  "description": "KORSANCIM için yerel Tailwind CSS derleme araçları — çalışma zamanında dotnet'in ihtiyacı yok, yalnızca stil değiştiğinde geliştirici burayı kullanır.",
  "scripts": {
    "build:css": "tailwindcss -i ./tailwind-input.css -o ./wwwroot/css/tailwind.css --minify",
    "watch:css": "tailwindcss -i ./tailwind-input.css -o ./wwwroot/css/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
